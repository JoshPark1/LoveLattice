{
  "name": "lovelattice-workspace",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -ws --if-present",
    "install:all": "npm install"
  }
}
